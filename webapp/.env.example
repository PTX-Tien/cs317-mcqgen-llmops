# Optional frontend overrides. Leave blank for the default:
# API  -> http://<current UI host>:8080
# WS   -> ws://<current UI host>:8080
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_WS_URL=

# Optional comma-separated dev origins if Next.js needs explicit hosts.
NEXT_ALLOWED_DEV_ORIGINS=
